<section class="row justify-content-center">
  <div class="col-lg-6">
    <div class="p-5 bg-white border rounded-3 text-center shadow-sm">
      <span class="badge text-bg-success mb-3"><%= t(".badge") %></span>
      <h1 class="h2 fw-bold"><%= t(".title") %></h1>
      <p class="text-secondary mt-3 mb-0"><%= t(".description", title: @attendance_list.title) %></p>
      <%= link_to t(".return_home"), root_path, class: "btn btn-primary mt-4" %>
    </div>
  </div>
</section>
